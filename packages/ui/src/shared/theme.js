const colors = {
  primary: '#ffffffff',
}

const fonts = {
  primary: 'Neon',
}

const buttons = {
  primary: {
    font: fonts.primary,
    background: colors.primary,
  },
}

const theme = {
  buttons,
}

export default theme
